name: 'Node app Build'
description: 'A GitHub Action that builds Node Application'
    
runs:
  using: 'composite'
  steps:
   - name: Install dependencies
     run:  npm install --prefix react
     shell: bash
     
   - name: Run build
     run: npm run build --prefix react
     shell: bash
  
  
