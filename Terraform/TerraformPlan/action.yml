name: 'Terraform Plan'
description: 'Terraform Compoite action for terraform plan'

runs:
  using: "composite"
  steps:
    - name: Terraform Plan
      run: terraform plan -input=false -no-color
      shell: bash
